// Archivo para lógica de express (Manejo de rutas, exportación del módulo y manejo de middleware)

const express = require('express');
const app = express();

// Declaración de rutas a ejecutar por express

// Inicio Middlewares

// Consumo de las rutas

// Fin Middlewares

// Exportación módulo
module.exports = app;